# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Keep Chinese localization as UTF-8 text in the working tree.
ReAstralPartyMod/localization/zhs/*.json text eol=lf working-tree-encoding=UTF-8
